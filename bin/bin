#!/usr/bin/env node

const { runComponents, runningComponents } = require('../src')

// todo move to utils or CLI
// Set config from CLI input

// Run
;(async () => {
  return runComponents()
})().catch((e) => console.log(e.stack)) // eslint-disable-line
