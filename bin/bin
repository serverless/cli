#!/usr/bin/env node

const { runComponents } = require('../new')
const publish = require('../new/commands/publish')

// Run
;(async () => {
  // return publish()
  return runComponents()
})().catch((e) => console.log(e.stack)) // eslint-disable-line
